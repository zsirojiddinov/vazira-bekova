# KKT tarjima tizimi — aniq versiyalar bilan qulflangan bog'liqliklar.
# Sinovdan o'tgan muhit: Python 3.14.0 (CPython, framework build, macOS
# Darwin 25.4.0 arm64), SQLite 3.51.0. Qarang: README.md.
#
# O'rnatish:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

# ── to'g'ridan-to'g'ri ishlatiladigan kutubxonalar ──
python-docx==1.2.0      # .docx manba fayllarni o'qish (load_bazalar_docx va h.k.)
openpyxl==3.1.5         # .xlsx affiks jadvallarini o'qish
nltk==3.10.3            # WordNetLemmatizer (USE_LEMMA=True bo'lsa)

# ── nltk/python-docx/openpyxl ning to'g'ridan-to'g'ri bog'liqliklari
#    (pip avtomatik o'rnatadi, lekin reproducibility uchun bu yerda ham
#    aniq versiya bilan qulflangan) ──
lxml==6.1.3
defusedxml==0.7.1
et_xmlfile==2.0.0
regex==2026.9.3
click==8.5.0
joblib==1.6.0
tqdm==4.70.0
typing_extensions==4.16.0

# ── test infratuzilmasi (Faza 0/1) ──
pytest==9.1.1
